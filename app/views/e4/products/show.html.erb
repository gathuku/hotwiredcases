<div>
  <h3 class="text-xl font-bold"><%= @product.name %> orders</h3>
  <div class="mt-5">
    <%= turbo_frame_tag :orders, src: e4_product_orders_path(@product), loading: :lazy do %>
      <%= render "skeleton" %>
    <% end %>
  </div>
</div>
